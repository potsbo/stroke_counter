require "spec_helper"

describe StrokeCounter do
  it "has a version number" do
    expect(StrokeCounter::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
